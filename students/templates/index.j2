{% extends 'base.j2' %}

{% block content %}

<ul>
{% if request.user %}
<h2>{{request.user.username}}</h2>

        <p>{{meta}}</p>
{% for i in meta %}
    <ul>
        <li>{{i.get_attname_column()}}</li>
    </ul>
{% endfor %}
{% endif %}
       <table>
        <th>{% #header %}</th>
        
        <td>
            {% for i in data %}

            {% endfor %}
        </td>
    </table>
</ul>

<form>
    {% csrf_token %}
    
</form>
{% endblock %}